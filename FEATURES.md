# MPM 기능 목록

> 최종 업데이트: 2026-07-10 (Render OOM 수정 / 보안 강화 — JWT 알고리즘 고정·IDOR 방어·공급망 감사)

---

## 1. 종목 추천 (`/stocks`)

| 기능 | 내용 |
|---|---|
| 추천 종목 표시 | 거래량 상위 30종목 → ETF 제외 → 기술적 분석 Top 10 |
| 즐겨찾기 탭 | 추천 목록 / 즐겨찾기 탭 전환, 탭 상태 URL 유지 |
| 즐겨찾기 토글 | ★ 아이콘으로 추가/제거 (Supabase `favorites` 테이블 저장, 환경별 공유) |
| 즐겨찾기 외 종목 | 추천 목록에 없는 즐겨찾기 종목은 KIS API에서 실시간 조회 후 동일 형식으로 표시 |
| 섹터 주도주 탭 | 20개 테마 섹터 선택 시 DB 캐시 반환 (캐시 없으면 빈 결과, "새로 불러오기"로 채움) |
| 종목명 검색 | stock_master 기반 KRX 전체 종목 검색 (ilike), KOSPI / KOSDAQ 배지 표시 |
| 추천 기준 | KIS 거래량 순위 + 듀얼 엔진 기술적 분석 스코어링 (0–100점, Engine A/B 중 높은 쪽 채택) |
| 자동 갱신 | 평일 08:50 (장 전) / 11:00 (오전 장중) / 14:00 (오후 장중) / 16:10 (장 후) 자동 업데이트 |
| 수동 동기화 | `POST /api/v1/stocks/sync/recommendations` |

### 섹터 주도주 상세

**지원 섹터 (20개)**

반도체(AI/HBM), 온디바이스 AI, 2차전지 소재·장비, 로봇·스마트팩토리, 우주항공·방산, 자율주행·전장부품, 바이오시밀러·신약, 양자컴퓨터·원자력(SMR), 자동차 제조, 조선·해양플랜트, 철강·비철금속, 화학·정유, 디스플레이·OLED, 기계·건설장비, 인터넷·엔터테인먼트, 게임·콘텐츠, 금융(은행·보험·증권), 음식료, 화장품·미용기기, 신재생 에너지

**섹터 점수 (100점)**

| 항목 | 배점 | 산출 방식 |
|---|---|---|
| 거래대금 | 30점 | 섹터 내 최대 거래대금 대비 비율 |
| 상승률 | 30점 | 섹터 내 최대 등락률 대비 비율 (하락 시 0점) |
| 정배열 | 20점 | 5MA > 20MA > 60MA 조건 충족 시 20점 |
| 시가총액 | 20점 | 500억 미만 Hard Filter 제외, 이상이면 20점 |

**기술 분석 점수 (오늘의 추천 알고리즘 동일 적용)**

| 항목 | 설명 |
|---|---|
| `tech_score` | Engine A/B 중 높은 쪽 (0–100점) |
| `engine_a_score` | 추세 돌파형 점수 |
| `engine_b_score` | 역추세 반등형 점수 |
| `tech_tags` | 분석 태그 (골든크로스·눌림목 반등 등) |

- OHLCV 130일·고가·저가·거래량 + 일목균형표 구름대 위치 + KOSPI 시장 레짐(BULL/BEAR) 반영
- 섹터 점수가 0이어도 tech_score > 0이면 분석 점수 배지 표시
- 종목명: `stock_master` DB 기준
- 결과 표: 순위 아이콘(👑🥈🥉), 종목명, 현재가, 등락률, 거래대금(억), 시총(억), 섹터 점수, 점수 구성 + 분석 점수 배지

**캐시 구조 (`sector_leaders` 테이블)**

| 컬럼 | 설명 |
|---|---|
| `sector` (PK) | 섹터명 |
| `data` | 주도주 상위 3개 JSONB 배열 |
| `updated_at` | 마지막 KIS API 조회 시각 |

**캐시 갱신 시점**

| 시점 | 방식 |
|---|---|
| 매일 09:05 KST (평일) | 스케줄러 자동 갱신 (20개 섹터 순차 처리) |
| "새로 불러오기" 버튼 | 사용자 수동 갱신 (`force=true`) |

- 캐시 미스 시 자동 조회 없음 → "새로 불러오기"로 명시적 갱신 필요
- KIS API 동시 호출 상한: 글로벌 세마포어 5개 (과부하 방지)
- "전체" 선택: `/sector-leader/all` 단일 DB 조회로 20개 섹터 일괄 반환

---

## 2. 종목 상세 (`/stocks/{code}`)

### 기준 정보

| 항목 | 내용 |
|---|---|
| 현재가 / 등락 | KIS API 실시간 (조회 시각 표시) |
| 오늘 OHLC | 시가 / 고가 / 저가 |
| 기준가 | 전일 종가 |
| 상한가 / 하한가 | ±30% 가격제한폭 |
| 52주 최고 / 최저 | 연간 가격 범위 |
| 시가총액 / 거래대금 | 조·억 단위 표시 |
| 외국인 보유율 | % |
| EPS / BPS | 가치 보조 지표 |
| 즐겨찾기 버튼 | 상세 페이지에서 즐겨찾기 추가/삭제 토글 (Supabase DB 반영) |
| 네이버 증권 링크 | 종목 상세 페이지에서 `네이버 증권 ↗` 버튼 — `finance.naver.com/item/main.naver?code={code}` 새 탭 오픈 |

### 기술적 분석 (스코어 0–100) — 듀얼 엔진 구조

두 개의 독립 엔진 중 높은 점수를 최종 스코어로 채택: `score = max(engine_a, engine_b)`

#### Stage 1 — Hard Filter

| 조건 | 처리 |
|---|---|
| MA20 거래량 < 100,000주 | 즉시 탈락 (score=0, 태그 없음) |

#### Engine A — 추세 돌파형 (최대 100점)

추세가 강하게 형성된 종목을 발굴. **BEAR 시장(KOSPI < MA20) 시 즉시 0점** — 하락장 가짜 돌파 방지.

| 항목 | 조건 | 배점 |
|---|---|---|
| 골든크로스 | MA5>MA20>MA60 정배열 → 15 / MA5>MA20만 → 8 | max 15 |
| ADX/DMI 강한 상승추세 | ADX≥25 → 15 / ADX≥20 → 10 | max 15 |
| 일목 구름대 돌파 | 구름 위 (above_cloud) → 15 / 구름 안 → 5 | max 15 |
| 볼린저 스퀴즈 상단돌파 | 밴드폭<10% + 현재가≥상단 → 15 / 밴드폭<10% + 현재가>중간 → 6 | max 15 |
| 전고점 돌파 | 21일 고점 돌파 + 거래량≥1.5× → 15 / 거래량 미충족 → 8 | max 15 |
| OBV 선행 돌파 | OBV>21일 OBV 고점 + 현재가≤주가 고점(선행) → 15 / OBV>고점만 → 8 | max 15 |
| 거래량 급증 | 거래량≥3.0×MA20 + 양봉 → 10 / ≥2.0× → 7 / ≥1.5× → 3 | max 10 |
| **RSI 3단계 처리** | Hard Veto: RSI≥80 → **즉시 0점** / 최초 70 돌파(직전<70) → +5 / 지속 70+ → max(0, score−10) | — |

#### Engine B — 역추세 반등형 (최대 100점)

과매도 구간에서 반등 포착. **프리필터 2개** 통과 후 B1/B2 구조로 분기.

**Pre-filter (미통과 시 즉시 0점)**

| 조건 | 이유 |
|---|---|
| 거래대금 < 50억 원 | 유동성 부족 — 진입·청산 어려움 |
| MA60 우하향 (현재 MA60 < 5봉 전 MA60) | 칼날 잡기 방지 — 장기 하락 추세 구간 제외 |

**B1 — 낙폭과대 V자 반등형 (disparity < 99, 만점 75 → 100 정규화)**

| 항목 | 조건 | 배점 |
|---|---|---|
| 이격도 저점 | <93%→15 / <95%→12 / <97%→8 / <99%→3 | max 15 |
| 과매도 그룹 (RSI/Stoch/CCI/MFI) | ≥3개→25 / ≥2개→20 / 1개→10 | max 25 |
| 수요밴드 통합 (볼린저·엔벨로프·피봇S2·피보나치) | ≥2개+양봉→35 / ≥2개→20 / 1개+양봉→20 / 1개→8 | max 35 |

**B2 — 정배열 눌림목형 (disparity ≥ 99, 만점 85 → 100 정규화)**

| 항목 | 조건 | 배점 |
|---|---|---|
| 과매도 그룹 (RSI/Stoch/CCI/MFI) | ≥3개→25 / ≥2개→20 / 1개→10 | max 25 |
| 수요밴드 통합 (볼린저·엔벨로프·피봇S2·피보나치) | ≥2개+양봉→35 / ≥2개→20 / 1개+양봉→20 / 1개→8 | max 35 |
| 눌림목 반등 | MA5>MA20 + 현재가>MA20 + 최근 5봉 MA20 ±2% 터치 + 양봉→25 / 양봉 미충족→12 | max 25 |

**강도 등급**

| 점수 범위 | 강도 |
|---|---|
| 75 – 100 | 매우 강함 |
| 50 – 74 | 강함 |
| 25 – 49 | 보통 |
| 0 – 24 | 약함 |

### 매수 신호 태그

엔진 판정 결과에 따라 종목 태그로 자동 부여됩니다.

| 태그 | 엔진 | 의미 |
|---|---|---|
| 추세 돌파형 | Engine A | Engine A 점수 ≥ Engine B — 추세 강도 기반 진입 |
| 역추세 반등형 | Engine B | Engine B 점수 > Engine A — 과매도 반등 기반 진입 |
| 골든크로스 | Engine A | MA 정배열 또는 MA5↑MA20 교차 |
| 강한 상승추세 | Engine A | ADX 기반 추세 강도 |
| 일목 구름대 돌파 | Engine A | 구름대 상단 위 |
| 볼린저 스퀴즈 상단돌파 | Engine A | 밴드 수축 후 상단 돌파 |
| 전고점 돌파 | Engine A | 21일 고점 돌파 + 거래량 확인 |
| OBV 선행 돌파 | Engine A | OBV가 주가에 선행하여 고점 돌파 |
| 거래량 급증 | Engine A/B | 거래량 MA20 대비 급증 + 양봉 |
| 이격도 저점 | Engine B | MA20 대비 현재가 괴리 과대 |
| 과매도 반등 | Engine B | RSI/Stoch/CCI/MFI 그룹 과매도 |
| 수요밴드 지지 | Engine B | 볼린저·엔벨로프·피봇·피보나치 복합 지지 |
| 눌림목 반등 | Engine B | 상승추세 유지 중 MA20 터치 후 반등 |

### 일목균형표

전환선 / 기준선 / 선행스팬 A·B / 구름 위·안·아래 위치

### 기본 가치 지표

PER / PBR / ROE

### 수익률 분석 (전략별)

| 항목 | 내용 |
|---|---|
| 분석 유형 | 퀀트(quant) / 배당(dividend) 탭 전환 |
| 실행 방식 | "분석 실행" 버튼 클릭 시 온디맨드 분석 (`GET /api/v1/analysis/{code}?strategy_type=…`) |
| 퀀트 결과 | 종합 점수(0–100), 방향성 힌트, 모멘텀·가치·변동성 3개 팩터 점수 |
| 배당 결과 | GGM 기대수익률, 배당수익률, D0/D1 추정, 성장률, 안정성 점수 및 등급 |
| 판정 | 진입 승인 / 진입 보류 + 사유 |

---

## 3. 추천 히스토리 (`/stocks/history`)

| 기간 | 내용 |
|---|---|
| 일별 | 최근 7일 추천 종목 |
| 주별 | 최근 4주 빈도 상위 10종목 집계 |
| 월별 | 최근 6개월 빈도 상위 10종목 집계 |
| 저장 시점 | 일일 동기화 시 자동 스냅샷 저장 |

---

## 4. 포트폴리오 (`/portfolio`)

### 투자 프로필 관리

| 기능 | 내용 |
|---|---|
| 프로필 생성 / 수정 / 삭제 | 이름(name) + 분석 유형(analysis_type) 설정 |
| 분석 유형 | `quant` (퀀트 분석) 또는 `dividend` (배당 분석) — AI 프롬프트 분기에 사용 |
| 프로필 전환 | 선택된 프로필 기준으로 보유 종목 및 AI 분석 전환 |

### 보유 종목 관리

| 기능 | 내용 |
|---|---|
| 등록 | 종목코드·명, 평균단가, 수량, 메모, profile_id |
| 인라인 편집 / 삭제 | 행에서 바로 수정 |
| 현재가 조회 | KIS API 직접 조회 (실시간) |
| 종목 타입·섹터 | KIS API `bstp_kor_isnm` 기반 종목 유형(ETF/ETN/리츠/주식) + 섹터명 표시 |
| 파이차트 | 보유 종목별 평가금액 비중 시각화 |

### 수익률 정보

| 항목 | 내용 |
|---|---|
| 매입금액 | 평균단가 × 수량 |
| 평가금액 | 현재가 × 수량 |
| 평가손익 | 평가금액 − 매입금액 |
| 수익률 | (현재가 − 평균단가) / 평균단가 × 100 |
| 등락률 | 당일 시세 등락 |
| 오늘 손익 | 당일 등락 기준 보유분 손익 |
| 합계 | 총 매입·평가·손익·수익률 |

### AI 포트폴리오 분석

| 항목 | 내용 |
|---|---|
| 모델 | Google Gemini 2.5 Flash Lite |
| 프롬프트 분기 | 프로필 `analysis_type`에 따라 퀀트(quant) / 배당(dividend) 전용 프롬프트 사용 |
| 캐시 | 보유 종목 구성 해시(holdings_hash) 기반 일 1회 캐시 (`portfolio_analyses` 테이블) |
| 갱신 조건 | 보유 종목 변경(추가·수정·삭제) 시 hash 변경 → 자동 재분석 |

### 매도 분석

- 페이지 로드 시 전 종목 자동 분석, 아이콘 색으로 즉시 표시
- 클릭 시 상세 패널 펼침

**매도 점수 등급**

| 점수 | 등급 | 아이콘 색 |
|---|---|---|
| 0–20 | 관망 | 초록 |
| 21–40 | 주의 | 노란색 |
| 41–65 | 매도 검토 | 주황색 |
| 66–100 | 즉시 매도 | 빨간색 |

**신호 분류**

| 분류 | 신호 |
|---|---|
| 기술적 | MA 데드크로스, RSI·스토캐스틱 과매수, 장대음봉 + 거래량, MACD 데드크로스, MA 하회 |
| 기본적 | PER·PBR 과열 |
| 자산관리 | 손절 (−5% / −10%), 트레일링 스탑 (−7%), 포트폴리오 비중 초과 |

**매도 가격대**

| 항목 | 기준 |
|---|---|
| 손절가 | 매수가 기준 −5% / −10% / −15% |
| 트레일링 스탑 | 최근 60일 고점 기준 −7% / −10% |
| 목표가 | EPS × PER 15 / 20 / 25배 |
| 참고선 | 52주 최고가 |

---

## 5. 가상 거래 (`/virtual`)

알고리즘 기반 자동 매매를 시뮬레이션하는 가상 거래 계좌 관리 기능.

### 가상 계좌 설정

| 설정 | 내용 |
|---|---|
| 전략 | `engine_a` (추세 돌파형) / `engine_b` (역추세 반등형) / `both` (A 또는 B) / `both_and` (A 및 B 동시 충족) |
| 점수 필터 유형 | `gte` (min_score 이상, 기본) / `lte` (max_score 이하) / `range` (min~max 범위) |
| 매수 최소 점수 | `min_score` — 기술 점수 하한 (기본 50) |
| 매수 최대 점수 | `max_score` — `lte`/`range` 전략 시 상한 (선택) |
| 최대 종목 수 | 동시 보유 최대 종목 수 (기본 5개) |
| 종목당 투자 비율 | 초기 자금 대비 % (기본 20%) |
| 손절 기준 | 매입가 대비 하락 % 초과 시 자동 손절 (기본 10%) |
| 익절 기준 | 매입가 대비 상승 % 초과 시 자동 익절 (기본 20%) |
| 최대 보유 일수 | `max_hold_days` — 초과 시 `max_hold_exit` 강제 청산 (선택, Engine A/B 공통) |
| 대형주 제외 | `filter_excl_large_cap` — 시가총액 임계값(억원) 초과 종목 매수 제외 (Engine B 리스크 관리) |
| 고유동성 제외 | `filter_excl_high_amount` — 일 거래대금 임계값(억원) 초과 종목 매수 제외 (Engine B) |

### 알고리즘 매매 트리거

**매매 트리거는 두 가지 방식으로 실행됩니다:**

**① 장중 10분 단위 트리거 (로컬 전용, ENABLE_INTRADAY=true)**

| 시간 | 내용 |
|---|---|
| 09:00~15:20 KST 매 10분 | KIS API 실시간 가격으로 매도·매수 순차 실행 |

**② 일일 동기화 연계 트리거 (Render, ENABLE_SCHEDULER=true)**

| 시각 (KST) | 내용 |
|---|---|
| 11:00 / 14:00 / 16:10 | 추천 갱신 후 매도·매수 트리거 순차 실행 |

**매도 트리거** (`virtual_sell_trigger`) — 먼저 실행:

**공통 (전략 무관)**

| 조건 | trigger_type | 처리 |
|---|---|---|
| 현재가 하락률 ≥ `stop_loss_pct` | `stop_loss` | 전량 매도 |
| 현재가 상승률 ≥ `take_profit_pct` | `take_profit` | 전량 매도 |
| 보유일수 ≥ `max_hold_days` | `max_hold_exit` | 강제 청산 전량 매도 (계좌에 설정된 경우만) |

**Engine A 전용**

| 조건 | trigger_type | 처리 |
|---|---|---|
| 현재가 < 진입가 − 1.5×entry_ATR | `atr_hard_stop` | 즉시 손절 전량 매도 |
| 현재가 < 최고가 − 2.0×현재ATR | `atr_trailing_stop` | 트레일링 스탑 전량 매도 |
| RSI가 70+ → 70 이하로 하향 교차 | `rsi_exhaustion` | 모멘텀 소멸 전량 매도 |

**Engine B 전용**

| 조건 | trigger_type | 처리 |
|---|---|---|
| 현재가 < 매수 당일 저가 | `entry_low_breach` | 전략 전제 붕괴 즉시 손절 |
| 보유 5봉 이상 + 현재가 ≤ 진입가 | `time_limit_stop` | 기회비용 청산 전량 매도 |
| MA20 최초 터치 (아래→위 교차) | `ma20_half_exit` | 분할 익절 50% 매도, `half_exited=true` |
| 이격도 ≥ 102% 또는 RSI ≥ 60 | `target_reached` | 목표 도달 전량 매도 |

**매수 트리거** (`virtual_buy_trigger`) — 매도 후 실행:

| 조건 | 처리 |
|---|---|
| 매수 대상 | 오늘 추천 종목 중 전략·최소점수 필터 통과 종목 |
| 제외 조건 | 이미 보유 중 / 당일 손절 종목 (재매수 금지) |
| 포지션 한도 | `max_positions` 미만일 때만 매수 |
| 체결가 | KIS API 현재가 기준 |
| 주문 금액 | 잔고 × `position_size / 100` |

**매도 신호 분류 (포트폴리오 페이지 표시용)**

| 분류 | 신호 |
|---|---|
| 기술적 | MA 데드크로스, RSI·스토캐스틱 과매수, 장대음봉+거래량, MACD 데드크로스, MA 하회 |
| 기본적 | PER·PBR 과열 |
| 자산관리 | 손절(−5%/−10%), 트레일링 스탑(−7%), 포트폴리오 비중 초과 |
| **공통(가상)** | 고정 손절·익절, 최대 보유일수 초과 청산 |
| **엔진A** | ATR 하드 스탑, ATR 트레일링 스탑, RSI 모멘텀 소멸 |
| **엔진B** | 보유 기간 손절(5봉), MA20 분할 익절, 이격도/RSI 목표 도달, 진입 저점 이탈 |

### 계좌 성과 요약

| 항목 | 내용 |
|---|---|
| 총 손익 | 실현 PnL 합계 + 미실현 평가손익 |
| 수익률 | 초기 자금 대비 |
| 승률 | 매도 체결 건 중 PnL > 0 비율 |
| 보유 포지션 | 종목별 평가손익·현재가 |
| 체결 내역 | trigger_type별 매수·매도 이력 |

---

## 6. 인프라 / 공통

### 스케줄러 (평일만 실행)

**ENABLE_SCHEDULER=true (일일 동기화 잡)**

| 시각 (KST) | 작업 |
|---|---|
| 서버 시작 시 | 오늘 추천 데이터 없으면 즉시 동기화 (1회) |
| 08:50 | 장 전 추천 종목 갱신 + 텔레그램 리포트 |
| 09:05 | 섹터 주도주 전체 20개 갱신 |
| 11:00 | 오전 장중 추천 갱신 + 가상매매 트리거 + 텔레그램 리포트 |
| 14:00 | 오후 장중 추천 갱신 + 가상매매 트리거 + 텔레그램 리포트 |
| 16:10 | 장 후 추천 갱신 + OHLCV 동기화 + 히스토리 저장 + 가상매매 트리거 + 텔레그램 리포트 |

**ENABLE_INTRADAY=true (장중 실시간 트리거, 로컬 전용)**

| 시각 (KST) | 작업 |
|---|---|
| 09:00~15:20 매 10분 | KIS 실시간 가격 → 매도(손절·익절·매도신호) → 매수(알고) |

> **현재 운영 구성 (2026-07~):** Render: `ENABLE_SCHEDULER=true` (일일 동기화), 로컬: `ENABLE_INTRADAY=true` + `ENABLE_TELEGRAM=true` (장중 트리거 + 텔레그램).

**텔레그램 알림 (`ENABLE_TELEGRAM=true`, 로컬 전용)**

| 알림 종류 | 전송 시점 | 내용 |
|---|---|---|
| 추천 종목 리포트 | 일일 동기화 완료 후 | 종목별 수급 조건·추천가·엔진 점수·태그 |
| 가상 매수 체결 | 알고리즘 매수 체결 즉시 | 계좌명·종목·엔진·점수·가격·수량·잔여현금 |
| 가상 매도 체결 | 알고리즘 매도 체결 즉시 | 계좌명·종목·매도가·손익금액·손익률·트리거·잔여현금 |

Render 배포 시 미전송 (환경변수 미설정으로 자동 비활성).

### 데이터베이스 (Supabase)

| 테이블 | 용도 |
|---|---|
| `stock_recommendations` | 추천 종목 캐시 (당일, tech_score 포함) |
| `stock_ohlcv` | 일별 OHLCV 2년치 |
| `recommendation_history` | 추천 히스토리 스냅샷 (일·주·월별, JSONB) |
| `holdings` | 보유 종목 (profile_id 연결) |
| `profiles` | 투자 프로필 (name, analysis_type: quant\|dividend) |
| `portfolio_analyses` | AI 포트폴리오 분석 캐시 (holdings_hash 기반) |
| `stock_master` | KRX 전체 상장 종목 (KOSPI 838 + KOSDAQ 1,819 = 2,657건) |
| `sector_leaders` | 섹터 주도주 캐시 (sector PK, data JSONB, updated_at) |
| `favorites` | 즐겨찾기 종목 (stock_code PK, stock_name, created_at) |
| `virtual_accounts` | 가상 거래 계좌 (전략·자금·손절익절 설정) |
| `virtual_positions` | 가상 보유 포지션 (account_id 연결, UNIQUE: account+stock_code, entry_atr·highest_price·half_exited·entry_low 포함) |
| `virtual_trades` | 가상 체결 내역 (매수·매도, trigger_type, PnL) |
| `reports` / `keyword_subscriptions` | ARA 프로젝트에서 관리 (동일 Supabase DB 공유) |

모든 테이블 RLS 활성화 (`020_enable_rls.sql`) — anon 키 기본 거부, service_role 우회

### 외부 연동

| 서비스 | 용도 |
|---|---|
| KIS API | 현재가, OHLCV, 거래량 순위, 종목 상세 (PER/PBR 등), 지수 차트 (KOSPI/KOSDAQ), 기관·외국인 수급 순위 |
| Gemini 2.5 Flash Lite | AI 포트폴리오 분석 |
| KRX kind.krx.co.kr | 전체 상장 종목 목록 동기화 |
| Telegram Bot API | 추천 종목 리포트 전송 (로컬 전용, `ENABLE_TELEGRAM=true`) |

### 보안

| 항목 | 내용 |
|---|---|
| JWT 인증 | Supabase JWT(Bearer) 검증, ES256(JWKS) / HS256(secret) 알고리즘 **명시 고정** (알고리즘 혼용 공격 방어) |
| JWKS TTL | 1시간마다 공개키 재조회 (키 로테이션 대응) |
| IDOR 방어 | holdings·profiles·virtual_accounts 등 모든 리소스 수정·삭제 시 소유권 검증 (타 사용자 id로 접근 차단) |
| 사용자 화이트리스트 | `ALLOWED_USER_EMAIL` 설정 시 해당 계정만 API 접근 허용 |
| CORS | `ALLOWED_ORIGINS` 환경변수로 허용 도메인 제한 |
| RLS | 전체 테이블 RLS 활성화 — anon 키 PostgREST 직접 접근 차단 |
| Rate Limiting | SlowAPI 미들웨어 — 120 req/min 초과 시 429 응답 |
| 공급망 감사 | `backend/security_audit.sh` — CVE 스캔(pip-audit) + 해시 무결성 + 버전 불일치 검사 |

### UX

| 기능 | 내용 |
|---|---|
| 뒤로가기 상태 유지 | 즐겨찾기·포트폴리오에서 상세 진입 후 복귀 시 탭·목록 상태 유지 |
| 조회 시각 표시 | 종목 상세 현재가에 데이터 기준 시각 표시 |
| 네이버 증권 링크 | 종목 목록(오늘의 추천·섹터 주도주·즐겨찾기)·포트폴리오 보유 종목에 `N ↗` 버튼 표시 → `finance.naver.com` 새 탭 오픈 |
| 체결 내역 날짜+시간 | 가상거래 체결 내역 날짜 셀에 날짜(위)와 KST 시간(아래) 2행 표시 (`created_at` UTC → KST 변환) |
| 가상거래 계좌 편집 | 계좌 카드의 ✎ 버튼으로 EditAccountModal 열기 — 계좌명·전략·점수 필터·손절익절 등 모든 설정 변경 (초기자금 제외) |
| 로그인 로딩 보장 | 로그인 성공 ~ 페이지 진입까지 "로그인 중..." 상태 유지 — Supabase 세션 전파 완료 후 AuthProvider가 `/stocks`로 리다이렉트 |
| 인증 양방향 가드 | AuthProvider: 세션 없으면 `/login`으로, 세션 있으면 `/login`에서 `/stocks`로 자동 이동 |

---

## 7. 시장 현황 (`/market`)

실시간 시장 지표, 주도주 랭킹, 종목 히트맵을 제공하는 3탭 구성.

### 탭 구조

| 탭 | 컴포넌트 | 설명 |
|---|---|---|
| 대시보드 | `MarketDashboard` | 8개 지수 카드 + 수급 현황 |
| 주도주 랭킹 | `MarketRankings` | 8개 카테고리 각 상위 5개 |
| 히트맵 | `TreemapHeatmap` | Squarified 트리맵 히트맵 |

### 대시보드 탭

**지수 카드 (3섹션)**

| 섹션 | 구성 종목 | 데이터 소스 |
|---|---|---|
| 국내 지수 | KOSPI, KOSDAQ | KIS API (`FHKUP03500100` output1) |
| 미국 지수 | NASDAQ, 다우존스, S&P 500 | Yahoo Finance (`^IXIC`, `^DJI`, `^GSPC`) |
| 환율·원자재·금리 | USD/KRW, WTI 유가($), 미국 10년물(%) | Yahoo Finance (`KRW=X`, `CL=F`, `^TNX`) |

각 카드: 현재가 · 등락폭 · 등락률 · 상승/하락 색상 (빨강/파랑)  
캐시: 1분 in-memory TTL

**수급 현황 (상위 50종목)**

| 항목 | 설명 |
|---|---|
| 외국인 순매수 | KOSPI 기관·외국인 합산 순매수 상위 50종목의 외국인 합계(주) |
| 기관 순매수 | 동일 종목 기준 기관 합계(주) |
| 개인 순매수 | 동일 종목 기준 개인 합계(주) |

### 주도주 랭킹 탭

KIS API 6개 병렬 호출 후 8개 카테고리 각 5종목 표시. 캐시: 2분 in-memory TTL

| 카테고리 키 | 표시명 | KIS API | 정렬 기준 |
|---|---|---|---|
| `rise` | 상승률 상위 | `FHPST01700000` (거래량 순위) | 등락률 내림차순 |
| `fall` | 하락률 상위 | 동일 | 등락률 오름차순 |
| `volume` | 거래량 상위 | `FHPST01700000` | 거래량 내림차순 |
| `amount` | 거래대금 상위 | `FHPST01700000` (거래대금 별도 호출) | 거래대금 내림차순 |
| `foreign_buy` | 외인 순매수 | `FHPTJ04400000` (`fid_etc_cls_code=1`) | 외인 순매수(주) 내림차순 |
| `institution_buy` | 기관 순매수 | `FHPTJ04400000` (`fid_etc_cls_code=2`) | 기관 순매수(주) 내림차순 |
| `high_52w` | 52주 신고가 | `FHPST01870000` (`fid_div_cls_code=1`) | API 반환 순서 |
| `low_52w` | 52주 신저가 | `FHPST01870000` (`fid_div_cls_code=2`) | API 반환 순서 |

각 카드: 순위 · 종목명/코드 · 현재가 · 등락률 · 카테고리별 지표(거래량/거래대금/순매수(주)/52주 고저가)

### 히트맵 탭

| 항목 | 내용 |
|---|---|
| 알고리즘 | Squarified Treemap (시가총액 기반 면적) |
| 표시 | 종목명·종목코드·등락률 3행 레이아웃 |
| 색상 | ±3% 기준 빨강(상승)~회색(보합)~파랑(하락) 그라디언트 |
| 정렬 | 등락률 / 외국인 순매수 / 기관 순매수 전환 |
| 호버 툴팁 | 현재가·등락률·시총·수급 + 최근 5일 스파크라인 |
| 클릭 | `/stocks/{code}` 상세 페이지 이동 |
| 종목 수 | SECTOR_STOCKS 유니버스 (약 100개 섹터 대표 종목) |
| 종목명 | `stock_master` DB 기준 한글명 |

### API 엔드포인트

| Method | Path | 설명 |
|---|---|---|
| `GET` | `/api/v1/market/indices` | 8개 지수 현재값 (1분 캐시) |
| `GET` | `/api/v1/market/rankings` | 주도주 8개 카테고리 랭킹 (2분 캐시, limit 파라미터) |
| `GET` | `/api/v1/market/treemap` | 트리맵 데이터 (3분 캐시, sort 파라미터) |
| `GET` | `/api/v1/market/index-chart` | 지수 차트 (market, period 파라미터) |
| `GET` | `/api/v1/market/investor-trend` | 기관·외국인 수급 집계 |
| `GET` | `/api/v1/market/adr` | 등락비율 시계열 (days 파라미터) |
| `GET` | `/api/v1/market/sparkline/{code}` | 종목 스파크라인 (최근 N일 종가) |
